<% provide(:title, 'All users') %>
<h1>Users of Blog app</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>,
      <%= link_to user.email, user %>
    </li>
  <% end %>
</ul>
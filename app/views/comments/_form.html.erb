<%= form_for ([@post, @comment]) do |f| %>
  <p>
  	<%= f.label :name %>
  	<%= f.text_field :name %></br>
  </p>

  <p>
  	<%= f.label :body %>
  	<%= f.text_area :body %></br>
  </p>

  <p>
  	<%= f.submit "Add a comment" %>
  </p>
<%end %>